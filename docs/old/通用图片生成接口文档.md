# 通用图片生成接口调用示例文档

## 接口基础信息

- **基础URL**: `https://llm.ai-nebula.com/v1/images/generations`
- **认证方式**: Bearer Token
- **认证令牌**: `Bearer sk-xxxxxxxxxx`
- **接口功能**: 支持文生图、图生图,谷歌Nano banana 支持上下文图文生图

## 2. 简单文生图功能

生成一张"可爱的橙色小猫坐在花园里"的图片：

```bash
curl -X POST "https://llm.ai-nebula.com/v1/images/generations" \
  -H "Authorization: Bearer sk-Iia3/AM7jEGHAXnE3dcWWEQz0J7SirQ=" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gemini-2.5-flash-image-preview",
    "prompt": "一只可爱的橙色小猫坐在花园里，阳光明媚，高质量摄影",
    "response_format": "b64_json"
  }'
```

## 3. 图生图功能

基于基础图像生成风景画：(image:url或Base64输入)
```bash
curl -X POST "${BASE_URL}/v1/images/generations" \
  -H "Authorization: Bearer sk-Iia3/AM7jEGHAXnE3dcWWEQz0J7SirQ=" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gemini-2.5-flash-image-preview",
    "response_format": "b64_json",
    "contents": [
    {
      "role": "user",
      "parts": [
        {
          "text": "根据这张图帮我生成一张俯瞰广州塔的图片"
        },
        {
          "image": "data:image/png;base64,iVBORw0KGgoAAxxxx[已截断]"
        }
      ]
    }
  ]
  }'
```

Nano banana同时支持上下文对话

```bash
curl -X POST "https://llm.ai-nebula.com/v1/images/generations" \
  -H "Authorization: Bearer sk-Iia3/AM7jEGHAXnE3dcWWEQz0J7SirQ=" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gemini-2.5-flash-image-preview",
    "response_format": "b64_json",
    "contents": [
    {
      "role": "user",
      "parts": [
        {
          "text": "根据这张图帮我生成一张俯瞰广州塔的图片"
        },
        {
          "image": "data:image/png;base64,iVBORw0KGgoAAxxxx[已截断]"
        }
      ]
    },
    {
      "role": "model",
      "parts": [
        {
          "text": "已为您生成1张图片，优化后的提示词：没问题，请看：\n`"
        },
        {
          "image": "data:image/png;base64,iVBORw0KGgoAAxxxx[已截断]"
        }
      ]
    },
    {
      "role": "user",
      "parts": [
        {
          "text": "把景色换成夜色下雪的场景"
        }
      ]
    }
  ]
  }'
```


## 响应处理说明

1. 成功响应（状态码 200）会返回包含图像数据的 JSON
2. 当 `response_format` 设为 `b64_json` 时，图像数据在 `data[].b64_json` 字段中
3. 当 `response_format` 设为 `url` 时，图像地址在 `data[].url` 字段中

### 保存Base64图像数据（命令行示例）

```bash
{
    "code": 200,
    "msg": "操作成功",
    "data": {
        "data": [
            {
                "url": "",
                "b64_json": "iVBORw0KGgoAAAANSUhEUgAABAAAAAQA[base截断]",
                "revised_prompt": ""
            }
        ],
        "created": 1757320007
    }
}
```